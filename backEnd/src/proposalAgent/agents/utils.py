async def scrapeInternetData(toolJson: list[dict], url: str):
    """
    Scrapes data from the internet.
    Uses the scrape tool and serper tool functions to gather information about a company.
    """
